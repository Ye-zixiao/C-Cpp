#include<iostream>
#include<string>
#include<vector>
#include<map>
#include<list>
#include<set>
using namespace::std;

int main(void)
{
	map<string, list<size_t>> word_list;
	map<vector<int>::iterator, int> iter_int;
	map<list<int>::iterator, int> ilist_iter_int;

	return 0;
}
